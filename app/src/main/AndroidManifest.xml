<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.attwatch">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Include required permissions for Google Mobile Ads to run -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.attwatch.activities.MainActivity"
            android:label="@string/app_name" />

        <receiver android:name="com.attwatch.helpers.notify" />
        <receiver android:name="com.attwatch.helpers.BunkPlanNotifier" />
        <receiver android:name="com.attwatch.helpers.onBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.attwatch.activities.Settings"
            android:label="@string/title_activity_settings"
            android:parentActivityName="com.attwatch.activities.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.attwatch.activities.MainActivity" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.NOTIFICATION_PREFERENCES" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.attwatch.activities.IntroScreen"
            android:label="@string/app_name"
            android:screenOrientation="locked"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.attwatch.activities.HelpAFriend"
            android:label="@string/title_activity_help_afriend"
            android:parentActivityName="com.attwatch.activities.MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.attwatch.activities.MainActivity" />
        </activity>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.attwatch.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths" />
        </provider>

        <activity
            android:name="com.attwatch.activities.SubjectLog"
            android:label="@string/title_activity_subject_log"
            android:parentActivityName="com.attwatch.activities.MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.attwatch.activities.MainActivity" />
        </activity>
        <activity
            android:name="com.attwatch.activities.BunkPlanner"
            android:label="@string/title_activity_bunk_planner"
            android:parentActivityName="com.attwatch.activities.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.attwatch.activities.MainActivity" />
        </activity>
        <activity
            android:name="com.attwatch.activities.BunkPlannerDialogue"
            android:excludeFromRecents="true"
            android:theme="@style/AppTheme.UserDialog" />
        <activity
            android:name="com.attwatch.activities.EventPlanner"
            android:label="@string/title_activity_event_planner"
            android:parentActivityName="com.attwatch.activities.MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.attwatch.activities.MainActivity" />
        </activity>
    </application>

</manifest>